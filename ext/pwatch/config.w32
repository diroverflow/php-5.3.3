// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("pwatch", "for pwatch support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("pwatch", "enable pwatch support", "no");

if (PHP_PWATCH != "no") {
	EXTENSION("pwatch", "pwatch.c");
}

